log_format hermodr_safe '$remote_addr - $request_id [$time_local] "$request_method /v1/owntracks $server_protocol" $status $body_bytes_sent';
limit_req_zone $binary_remote_addr zone=hermodr_ingest:10m rate=2r/s;
