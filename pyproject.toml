[project]
name = "hermodr"
version = "0.1.0.dev0"
description = "Local-first deterministic location ingestion service"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hermodr = "hermodr.cli:main"

[build-system]
requires = []
build-backend = "hermodr_build_backend"
backend-path = ["."]

[tool.hermodr]
license-policy = "stdlib-only"
